#include "monty.h"

/**
 * doNothing - does nothing
 * @stackTop: pointer to the stack top
 * @lineNumber: line number
 * Return: no return
 */
void doNothing(stack_t **stackTop, unsigned int lineNumber)
{
	(void)stackTop;
	(void)lineNumber;
}
